{
  "_name": "test_lua_deprecated",
  "_desc": "Fixture for test_lua_deprecated: two EXPLICIT engine:\"lua\" nodes and NO allow_deprecated_lua, so SimController::initialize() must refuse it as authored. The test then flips the opt-in in memory and re-runs the same config to prove the sanctioned path works. Reuses bw_probe.lua as the driver (one TX, receiver never transmits).",
  "simulation": {
    "duration_ms": 3000,
    "step_ms": 1,
    "warmup_ms": 0,
    "seed": 4242,
    "node_startup_jitter_ms": 0,
    "radio": {
      "sf": 7,
      "bw": 125,
      "cr": 5,
      "duty_cycle": 100,
      "rx_preamble_miss_prob": 0.0,
      "decode_margin_steepness_db": 0.0,
      "snr_coherence_ms": 0
    }
  },
  "nodes": [
    { "name": "luasender", "engine": "lua", "script": "bw_probe.lua", "config": {} },
    { "name": "luahearer", "engine": "lua", "script": "bw_probe.lua", "config": {} }
  ],
  "topology": {
    "links": [
      { "from": "luasender", "to": "luahearer", "snr": 8.0, "rssi": -80.0, "snr_std_dev": 0 }
    ]
  },
  "commands": [
    { "at_ms": 1000, "node": "luasender", "command": "ping" }
  ]
}
